#include"HEAD.h"
#include"Cp.h"
using namespace std;
int main(){
    cin.tie(0);
    ios::sync_with_stdio(0);
    srand(time(0));
    Minmax::init();
    Minmax::pass(1);
    system("pause");
    return 0;
}
